import httpx

# 소셜 로그인 제공자로 나가는 모든 호출의 타임아웃.
#
# 지금까지는 httpx.AsyncClient()를 인자 없이 써서 라이브러리 기본값(5초)에
# 의존하고 있었다 — 무한 대기는 아니었지만(운영 파드에서 httpx 0.28.1 /
# Timeout(5.0) 확인) 코드 어디에도 5초라는 근거가 없어서, httpx를 올리다
# 기본값이 바뀌면 로그인 지연이 조용히 따라 바뀐다.
#
# read는 지금 동작을 그대로 유지하려고 5초로 고정한다. connect만 3초로 줄인다 —
# 제공자 호스트에 아예 닿지 않는 상황(DNS/네트워크)에서 5초를 기다릴 이유가
# 없고, 정상 응답에는 영향이 없다.
OAUTH_HTTP_TIMEOUT = httpx.Timeout(5.0, connect=3.0)

# python:3.12-slim(Debian) base image OS package CVEs.
# Risk-accepted 2026-07-12: ci_sandbox is a FastAPI app that does not invoke
# perl/gzip/acl/ncurses/util-linux at runtime - these are incidental base OS
# packages, not part of our app's attack surface. Several are also
# vendor "fix_deferred" (Debian has chosen not to patch yet), so no
# upstream fix exists to pull in via apt upgrade.
# Re-review this file whenever the Dockerfile base image changes.
#
# Re-reviewed 2026-07-15 after pinning python:3.12-slim to a digest (PR #16):
# two new perl-base CVEs appeared under the same already-accepted rationale
# above (perl is not invoked by ci_sandbox at runtime).

CVE-2026-13221
CVE-2026-57432
CVE-2025-69720
CVE-2026-41992
CVE-2026-42496
CVE-2026-42497
CVE-2026-48962
CVE-2026-53615
CVE-2026-54369
CVE-2026-8376
CVE-2026-9538

# sharp (npm, transitive via Next.js image optimization) - libvips inherited HIGH.
# GHSA-f88m-g3jw-g9cj / CVE-2026-33327, -33328, -35590, -35591.
# 2026-07-22: a Trivy DB update newly flagged this - the same image passed the scan
# hours earlier, i.e. a real new disclosure (the gate is working). A fix EXISTS
# (sharp 0.35.0). Temporarily accepted to unblock the 7/22 demo because regenerating
# package-lock.json (Dockerfile uses `npm ci`) requires npm, unavailable in the
# current environment. TRACKED: proper bump to sharp 0.35.0 in 재정비 (P1).
# Risk: transitive image-processing lib, not on a user-upload path. Re-remove after bump.
GHSA-f88m-g3jw-g9cj
CVE-2026-33327
CVE-2026-33328
CVE-2026-35590
CVE-2026-35591

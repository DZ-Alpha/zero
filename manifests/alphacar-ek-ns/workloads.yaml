# Generated from k8s_DangDang_시방서.xlsx — 자동 생성 스캐폴드
# 검토 필요 항목은 REVIEW.md 참고. 원본 설계는 docs/infra-spec.md 참고.
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: alphacar-elasticsearch
  namespace: alphacar-ek-ns
  labels:
    app: alphacar-elasticsearch
    app.kubernetes.io/part-of: alphacar
spec:
  serviceName: alphacar-elasticsearch-headless
  replicas: 1
  selector:
    matchLabels:
      app: alphacar-elasticsearch
  template:
    metadata:
      labels:
        app: alphacar-elasticsearch
    spec:
      containers:
      - name: elasticsearch
        image: docker.elastic.co/elasticsearch/elasticsearch:8.11.0
        ports:
        - containerPort: 9200
        resources:
          requests:
            memory: 1Gi
            cpu: 500m
          limits:
            memory: 1500Mi
            cpu: '1'
        volumeMounts:
        - name: elasticsearch-data
          mountPath: /usr/share/elasticsearch/data
  volumeClaimTemplates:
  - metadata:
      name: elasticsearch-data
    spec:
      accessModes:
      - ReadWriteOnce
      resources:
        requests:
          storage: 20Gi

# Generated from k8s_DangDang_시방서.xlsx — 자동 생성 스캐폴드
# 검토 필요 항목은 REVIEW.md 참고. 원본 설계는 docs/infra-spec.md 참고.
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: chaos-mesh
  namespace: alphacar-chaos-ns
  labels:
    app: chaos-mesh
    app.kubernetes.io/name: chaos-mesh
    app.kubernetes.io/part-of: alphacar
    tier: chaos
spec:
  replicas: 1
  selector:
    matchLabels:
      app: chaos-mesh
  template:
    metadata:
      labels:
        app: chaos-mesh
        app.kubernetes.io/name: chaos-mesh
        app.kubernetes.io/part-of: alphacar
        tier: chaos
      annotations:
        alphacar.io/config-file: chaos-mesh.conf
    spec:
      containers:
      - name: chaos-mesh
        image: chaos mesh
        resources:
          requests:
            cpu: 256m
          limits:
            cpu: 512m

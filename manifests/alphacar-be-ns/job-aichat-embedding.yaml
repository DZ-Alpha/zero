# Generated from k8s_DangDang_시방서.xlsx — 자동 생성 스캐폴드
# 검토 필요 항목은 REVIEW.md 참고. 원본 설계는 docs/infra-spec.md 참고.
---
apiVersion: batch/v1
kind: Job
metadata:
  name: aichat-embedding-job
  namespace: alphacar-be-ns
spec:
  ttlSecondsAfterFinished: 3600
  backoffLimit: 3
  template:
    spec:
      restartPolicy: Never
      containers:
      - name: aichat-embedding
        image: 192.168.0.169/bh/alphacar-aichat:1.0.12
        resources:
          requests:
            cpu: 500m
            memory: 1Gi
          limits:
            cpu: 2000m
            memory: 2Gi
        volumeMounts:
        - name: vector-store
          mountPath: /app/vector_store
      volumes:
      - name: vector-store
        persistentVolumeClaim:
          claimName: aichat-vector-store-pvc

# Generated from k8s_DangDang_시방서.xlsx — 자동 생성 스캐폴드
# 검토 필요 항목은 REVIEW.md 참고. 원본 설계는 docs/infra-spec.md 참고.
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: longhorn
  namespace: alphacar-backup-ns
  labels:
    app: longhorn
    app.kubernetes.io/name: longhorn
    app.kubernetes.io/part-of: alphacar
    tier: data
spec:
  replicas: 1
  selector:
    matchLabels:
      app: longhorn
  template:
    metadata:
      labels:
        app: longhorn
        app.kubernetes.io/name: longhorn
        app.kubernetes.io/part-of: alphacar
        tier: data
      annotations:
        alphacar.io/config-file: nook.conf
    spec:
      containers:
      - name: longhorn
        image: longhorn
        resources:
          requests:
            cpu: 500m
            memory: 1.5Gi
          limits:
            cpu: '2'
            memory: 4Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: velero
  namespace: alphacar-backup-ns
  labels:
    app: velero
    app.kubernetes.io/name: velero
    app.kubernetes.io/part-of: alphacar
    tier: data
spec:
  replicas: 1
  selector:
    matchLabels:
      app: velero
  template:
    metadata:
      labels:
        app: velero
        app.kubernetes.io/name: velero
        app.kubernetes.io/part-of: alphacar
        tier: data
      annotations:
        alphacar.io/config-file: velero.conf
        alphacar.io/secret-ref: velero-secret
    spec:
      containers:
      - name: velero
        image: velero
        resources:
          requests:
            cpu: 256m
          limits:
            cpu: 512m

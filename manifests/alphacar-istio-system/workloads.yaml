# Generated from k8s_DangDang_시방서.xlsx — 자동 생성 스캐폴드
# 검토 필요 항목은 REVIEW.md 참고. 원본 설계는 docs/infra-spec.md 참고.
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: istio
  namespace: alphacar-istio-system
  labels:
    app: istio
    app.kubernetes.io/name: istio
    app.kubernetes.io/part-of: alphacar
    tier: network
spec:
  replicas: 1
  selector:
    matchLabels:
      app: istio
  template:
    metadata:
      labels:
        app: istio
        app.kubernetes.io/name: istio
        app.kubernetes.io/part-of: alphacar
        tier: network
      annotations:
        alphacar.io/config-file: istio-mesh.conf
    spec:
      containers:
      - name: istio
        image: istio
        resources:
          requests:
            cpu: 500m
            memory: 1Gi
          limits:
            cpu: '2'
            memory: 2Gi
